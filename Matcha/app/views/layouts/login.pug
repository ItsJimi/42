extends ../index.pug

block content
	login
		input(id="signin_mail", type="text", placeholder="Mail")
		input(id="signin_pass", type="password", placeholder="Password")
		button#signin_submit
			| SignIn
